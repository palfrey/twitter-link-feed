---
applications:
  .:
    name: abbreviated-twitter-feed
    framework:
      name: django
      info:
        mem: 128M
        description: Python Django Application
        exec: 
    infra: eu-aws
    url: ${name}.${target-base}
    mem: 128M
    instances: 1
    services:
      mysql:
        type: mysql
